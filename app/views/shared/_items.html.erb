<li class="items-entry">
  <div class="item-details">
    <div class="item-text">
      <% if date %>
        <div class="item-name"><%= date_helper itemizables[:created_at] %></div>
        <div class="item-date"><%= itemizables[:habit] %></div>
      <% else %>
        <div class="item-name"><%= itemizables[:habit] %></div>
        <div class="item-date"></div>
      <% end %>
    </div>
    <div class="item-app">
    <% if itemizables[:time] %>
      <%= to_time_format(itemizables[:time]) %>
    <% end %>
  </div>
  </div>
  <% if linkable %>
    <%= link_to nil, itemizables, class: 'item-link-cover' %>
  <% end %>
</li>